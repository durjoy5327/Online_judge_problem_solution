#include <bits/stdc++.h>
using namespace std;
vector<int> mergesort(vector<int>& arr){
    if(arr.size()==1) return arr;
    int mid =arr.size()/2;
    vector<int>left=mergesort();
    
}

int main() {
   int arr[]={5,2,3,4,5,9,10,7};
   int n=8;

    return 0;
}